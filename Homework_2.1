using System;

namespace Homework_2._1
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Введите массу первого тела");
            decimal weight1 = decimal.Parse(Console.ReadLine());

            Console.WriteLine("Введите объём первого тела");
            decimal volume1 = decimal.Parse(Console.ReadLine());

            Console.WriteLine("Введите массу второго тела");
            decimal weight2 = decimal.Parse(Console.ReadLine());

            Console.WriteLine("Введите объём второго тела");
            decimal volume2 = decimal.Parse(Console.ReadLine());

            decimal d1 = weight1 / volume1;
            decimal d2 = weight2 / volume2;

            if (d1>d2)
            {
                Console.WriteLine("Плотность первого тела больше.");
            }
            if (d1 < d2)
            {
                Console.WriteLine("Плотность второго тела больше");
            }
            if (d1 == d2)
            {
                Console.WriteLine("Плотность тел равна.");
            }
        }
    }
}
